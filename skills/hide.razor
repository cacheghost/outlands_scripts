if not timerexists 'hiding'
    createtimer 'hiding'
    settimer 'hiding' 20000
endif
while not hidden
    if timer 'hiding' > 10000
        hotkey 'Hiding'
        settimer 'hiding' 0
    endif
endwhile
script main