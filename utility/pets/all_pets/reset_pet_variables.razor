# Tank 1 - 2
if varexist 'tank1'
    unsetvar 'tank1'
endif

if varexist 'tank2'
    unsetvar 'tank2'
endif

# DPS 1 - 4
if varexist 'dpspet1'
    unsetvar 'dpspet1'
endif

if varexist 'dpspet1'
    unsetvar 'dpspet1'
endif

if varexist 'dpspet2'
    unsetvar 'dpspet2'
endif

if varexist 'dpspet3'
    unsetvar 'dpspet3'
endif

if varexist 'dpspet4'
    unsetvar 'dpspet4'
endif