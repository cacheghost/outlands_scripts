# Tank 1 - 2
if varexist 'tank1'
    @unsetvar 'tank1'
    overhead '[Unset Tank 1]' 33
endif

# DPS 1 - 4
if varexist 'dpspet1'
    @unsetvar 'dpspet1'
    overhead '[Unset DPS 1]' 33
endif

if varexist 'dpspet2'
    @unsetvar 'dpspet2'
    overhead '[Unset DPS 2]' 33
endif

if varexist 'dpspet3'
    @unsetvar 'dpspet3'
    overhead '[Unset DPS 3]' 33
endif

if varexist 'dpspet4'
    @unsetvar 'dpspet4'
    overhead '[Unset DPS 4]' 33
endif

script main