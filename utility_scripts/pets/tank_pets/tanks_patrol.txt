wait 150
if not varexist 'tank1'
    overhead 'Select tank pet 1'
    setvar! 'tank1'
endif

if not varexist 'tank2'
    overhead 'Select tank pet 2'
    setvar! 'tank2'
endif

# Follow
menu 'tank1' 3
wait 150
menu 'tank2' 3
wait 150