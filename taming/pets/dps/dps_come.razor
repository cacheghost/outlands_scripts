overhead '[DPS Come]' 44
# Come
if skill 'Herding' >= 1
    if varexist 'dpspet1'
        if dead 'dpspet1'
            menu 'dpspet1' 5
            wait 100
        else
            menu 'dpspet1' 6
            wait 100
        endif
    endif
    if varexist 'dpspet2'
        if dead 'dpspet2'
            menu 'dpspet2' 5
            wait 100
        else
            menu 'dpspet2' 6
            wait 100
        endif
    endif
    if varexist 'dpspet3'
        if dead 'dpspet3'
            menu 'dpspet3' 5
            wait 100
        else
            menu 'dpspet3' 6
            wait 100
        endif
    endif
    if varexist 'dpspet4'
        if dead 'dpspet4'
            menu 'dpspet4' 5
            wait 100
        else
            menu 'dpspet4' 6
            wait 100
        endif
    endif
else
    if varexist 'dpspet1'
        if dead 'dpspet1'
            menu 'dpspet1' 4
            wait 100
        else
            menu 'dpspet1' 5
            wait 100
        endif
    endif
    if varexist 'dpspet2'
        if dead 'dpspet2'
            menu 'dpspet2' 4
            wait 100
        else
            menu 'dpspet2' 5
            wait 100
        endif
    endif
    if varexist 'dpspet3'
        if dead 'dpspet3'
            menu 'dpspet3' 4
            wait 100
        else
            menu 'dpspet3' 5
            wait 100
        endif
    endif
    if varexist 'dpspet4'
        if dead 'dpspet4'
            menu 'dpspet4' 4
            wait 100
        else
            menu 'dpspet4' 5
            wait 100
        endif
    endif
endif
script main