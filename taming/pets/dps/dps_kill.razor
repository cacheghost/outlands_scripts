@unsetvar 'allkill_taget'
overhead '[DPS Kill]' 44
@setvar! 'allkill_target'

# Follow
if skill 'Herding' >= 50
    if varexist 'dpspet1'
        menu 'dpspet1' 3
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 3
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 3
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 3
        wait 50
        target 'allkill_target'
    endif
else
    if varexist 'dpspet1'
        menu 'dpspet1' 2
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 2
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 2
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 2
        wait 50
        target 'allkill_target'
    endif
endif
script main