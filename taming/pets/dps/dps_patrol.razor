overhead '[DPS Patrol]' 44
# Follow
if skill 'Herding' >= 1
    if varexist 'dpspet1'
        menu 'dpspet1' 3
        wait 100
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 3
        wait 100
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 3
        wait 100
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 3
        wait 100
    endif
else
    if varexist 'dpspet1'
        menu 'dpspet1' 2
        wait 100
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 2
        wait 100
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 2
        wait 100
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 2
        wait 100
    endif
endif
script main