# Tank 1 - 2
if not varexist 'tank1'
    overhead 'Select tank pet 1'
    @setvar! 'tank1'
    wait 150
    if not varexist 'tank1'
        overhead '[Ignore Tank 1]' 33
    endif
endif

# DPS 1 - 4
if not varexist 'dpspet1'
    overhead 'Select dps pet 1'
    @setvar! 'dpspet1'
    wait 150
    if not varexist 'dpspet1'
        overhead '[Ignore DPS 1]' 33
    endif
endif

if not varexist 'dpspet2'
    overhead 'Select dps pet 2'
    @setvar! 'dpspet2'
    wait 150
    if not varexist 'dpspet2'
        overhead '[Ignore DPS 2]' 33
    endif
endif

if not varexist 'dpspet3'
    overhead 'Select dps pet 3'
    @setvar! 'dpspet3'
    wait 150
    if not varexist 'dpspet3'
        overhead '[Ignore DPS 3]' 33
    endif
endif

if not varexist 'dpspet4'
    overhead 'Select dps pet 4'
    @setvar! 'dpspet4'
    wait 150
    if not varexist 'dpspet4'
        overhead '[Ignore DPS 4]' 33
    endif
endif