overhead '[All Move]' 44
# Follow
if skill 'Herding' >= 1
    if varexist 'tank1'
        menu 'tank1' 7
        wait 100
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 7
        wait 100
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 7
        wait 100
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 7
        wait 100
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 7
        wait 100
    endif
else
    if varexist 'tank1'
        menu 'tank1' 6
        wait 100
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 6
        wait 100
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 6
        wait 100
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 6
        wait 100
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 6
        wait 100
    endif
endif
script main