overhead '[All Stay]' 44
# Stay
if skill 'Herding' >= 1
    if varexist 'tank1'
        if dead 'tank1'
            menu 'tank1' 6
            wait 100
        else
            menu 'tank1' 8
            wait 100
        endif
    endif
    if varexist 'dpspet1'
        if dead 'dpspet1'
            menu 'dpspet1' 6
            wait 100
        else
            menu 'dpspet1' 8
            wait 100
        endif
    endif
    if varexist 'dpspet2'
        if dead 'dpspet2'
            menu 'dpspet2' 6
            wait 100
        else
            menu 'dpspet2' 8
            wait 100
        endif
    endif
    if varexist 'dpspet3'
        if dead 'dpspet3'
            menu 'dpspet3' 6
            wait 100
        else
            menu 'dpspet3' 8
            wait 100
        endif
    endif
    if varexist 'dpspet4'
        if dead 'dpspet4'
            menu 'dpspet4' 6
            wait 100
        else
            menu 'dpspet4' 8
            wait 100
        endif
    endif
else
    if varexist 'tank1'
        if dead 'tank1'
            menu 'tank1' 6
            wait 100
        else
            menu 'tank1' 7
            wait 100
        endif
    endif
    if varexist 'dpspet1'
        if dead 'dpspet1'
            menu 'dpspet1' 6
            wait 100
        else
            menu 'dpspet1' 7
            wait 100
        endif
    endif
    if varexist 'dpspet2'
        if dead 'dpspet2'
            menu 'dpspet2' 6
            wait 100
        else
            menu 'dpspet2' 7
            wait 100
        endif
    endif
    if varexist 'dpspet3'
        if dead 'dpspet3'
            menu 'dpspet3' 6
            wait 100
        else
            menu 'dpspet3' 7
            wait 100
        endif
    endif
    if varexist 'dpspet4'
        if dead 'dpspet4'
            menu 'dpspet4' 6
            wait 100
        else
            menu 'dpspet4' 7
            wait 100
        endif
    endif
endif
script main