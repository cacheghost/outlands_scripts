@unsetvar 'allkill_target'
overhead '[All Kill]' 44
@setvar! 'allkill_target'

# Kill
if skill 'Herding' >= 1
    if varexist 'tank1'
        menu 'tank1' 2
        wait 100
        target 'allkill_target'
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 2
        wait 100
        target 'allkill_target'
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 2
        wait 100
        target 'allkill_target'
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 2
        wait 100
        target 'allkill_target'
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 2
        wait 100
        target 'allkill_target'
    endif
else
    if varexist 'tank1'
        menu 'tank1' 1
        wait 100
        target 'allkill_target'
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 1
        wait 100
        target 'allkill_target'
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 1
        wait 100
        target 'allkill_target'
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 1
        wait 100
        target 'allkill_target'
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 1
        wait 100
        target 'allkill_target'
    endif
endif
script main