overhead '[All Guard]' 44
# Guard
if skill 'Herding' >= 50
    if varexist 'tank1'
        menu 'tank1' 4
        wait 50
        target 'allkill_target'
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 4
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 4
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 4
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 4
        wait 50
        target 'allkill_target'
    endif
else
    if varexist 'tank1'
        menu 'tank1' 3
        wait 50
        target 'allkill_target'
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 3
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 3
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 3
        wait 50
        target 'allkill_target'
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 3
        wait 50
        target 'allkill_target'
    endif
endif
script main