overhead '[All Follow]' 44
# Follow
if skill 'Herding' >= 1
    if varexist 'tank1'
        menu 'tank1' 5
        wait 100
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 5
        wait 100
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 5
        wait 100
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 5
        wait 100
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 5
        wait 100
    endif
else
    if varexist 'tank1'
        menu 'tank1' 4
        wait 100
    endif
    if varexist 'dpspet1'
        menu 'dpspet1' 4
        wait 100
    endif

    if varexist 'dpspet2'
        menu 'dpspet2' 4
        wait 100
    endif

    if varexist 'dpspet3'
        menu 'dpspet3' 4
        wait 100
    endif

    if varexist 'dpspet4'
        menu 'dpspet4' 4
        wait 100
    endif
endif
script main