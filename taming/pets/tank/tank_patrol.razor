overhead '[Tank Patrol]' 44
# Follow
if skill 'Herding' >= 1
    if varexist 'tank1'
        menu 'tank1' 3
        wait 100
    endif
else
    if varexist 'tank1'
        menu 'tank1' 2
        wait 100
    endif
endif
script main