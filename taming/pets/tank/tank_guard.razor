overhead '[Tank Guard]' 44
if skill 'Herding' >= 50
    if varexist 'tank1'
        menu 'tank1' 4
        wait 50
    endif
else
    if varexist 'tank1'
        menu 'tank1' 3
        wait 50
    endif
endif
script main