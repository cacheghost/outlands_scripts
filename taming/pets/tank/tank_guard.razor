overhead '[Tank Guard]' 44
# Follow
if skill 'Herding' >= 1
    if varexist 'tank1'
        menu 'tank1' 4
        wait 100
    endif
else
    if varexist 'tank1'
        menu 'tank1' 3
        wait 100
    endif
endif
script main