overhead '[Tank Stay]' 44
# Stay
if skill 'Herding' >= 1
    if varexist 'tank1'
        if dead 'tank1'
            menu 'tank1' 6
            wait 100
        else
            menu 'tank1' 8
            wait 100
        endif
    endif
else
    if varexist 'tank1'
        if dead 'tank1'
            menu 'tank1' 6
            wait 100
        else
            menu 'tank1' 7
            wait 100
        endif
    endif
endif
script main