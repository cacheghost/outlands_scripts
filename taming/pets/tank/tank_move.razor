overhead '[Tank Move]' 44
# Move
if skill 'Herding' >= 1
    if varexist 'tank1'
        if dead 'tank1'
            menu 'tank1' 6
            wait 100
        else
            menu 'tank1' 7
            wait 100
        endif
    endif
else
    if varexist 'tank1'
        if dead 'tank1'
            menu 'tank1' 5
            wait 100
        else
            menu 'tank1' 6
            wait 100
        endif
    endif
endif
script main