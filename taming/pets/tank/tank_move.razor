overhead '[Tank Move]' 44
if skill 'Herding' >= 50
    if varexist 'tank1'
        menu 'tank1' 8
        wait 50
    endif
else
    if varexist 'tank1'
        menu 'tank1' 7
        wait 50
    endif
endif
script main