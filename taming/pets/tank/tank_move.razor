overhead '[Tank Move]' 44
# Follow
if skill 'Herding' >= 1
    if varexist 'tank1'
        menu 'tank1' 7
        wait 100
    endif
else
    if varexist 'tank1'
        menu 'tank1' 6
        wait 100
    endif
endif
script main